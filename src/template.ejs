<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
  </head>
  <body>
    <div id="main">
      <div>
        <h1 v-if="current !== ''">{{current}}</h1>
      </div>
      <button @click="randomize()">Click me!</button>
      <button @click="reset()">Reset</button>
    </div>
  </body>
</html>
